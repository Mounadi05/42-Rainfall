#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main(int ac, char **av)
{
    int n = atoi(av[1]);
    if (n == 423)
    {
        system("/bin/sh");
    }
    else
    {
        puts("No !");
    } 
}